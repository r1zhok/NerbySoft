spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: library-app
member:
  max:
    book:
      limit: 10